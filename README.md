# 🍃 Spring Boot Webserver

A lightweight starter for our first AI-augmented project.

## 🚀 Getting Started
1. **Clone & Open**: Open this folder in IntelliJ IDEA (2025.3 or later).
2. **Build**: Run `./mvnw clean install` to download dependencies.
3. **Run**: Launch `Application.java` or run `./mvnw spring-boot:run`.
4. **Access**: The server runs on `http://localhost:8080`.

## 🤖 Working with Junie (Our AI Agent)
This project is configured with **AI Guardrails** in `.aiassistant/rules/`. 
To get started, open the **AI Assistant** tab and try these prompts:

- **Planning**: `"@projectStructure explain the flow of a request in this app."`
- **Development**: `"Add a new GET endpoint /hello that returns a JSON greeting."`
- **Testing**: `"Generate a JUnit 5 test for the GreetingController."`

## 🛠 Project Rules
